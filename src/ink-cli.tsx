#!/usr/bin/env bun
import React from "react";
import { render } from "ink";
import App from "./ink-app.js";

// This entry point launches the new Ink-based CLI.
// Legacy CLI remains available at src/cli.ts.

render(<App />);